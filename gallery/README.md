# Acceptance gallery

Run `function integrateddynamics_gallery:build`, then render the staging map.
The six checks cover cable topology, native and extension parts, part-only
housings, stone/oak facades, and two stock controls. `#failures` in objective
`id_gallery` must remain zero. The corrected `0.1.0-alpha.1` gallery passed with
one invocation, six successes, and zero failures before owner acceptance on
2026-08-17.
